---
"firstrecall": patch
---

Fireworks AI's model dropdown now shows the short model name (e.g. "llama-v3p1-70b-instruct") instead of the full "accounts/.../models/..." ID. The full ID is still used as the underlying model value and appears as a detail line under the selected option.
