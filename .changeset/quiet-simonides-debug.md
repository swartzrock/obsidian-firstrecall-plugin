---
"firstrecall": patch
---

Add an opt-in developer console flag to log the next Simonides request and raw response, including transport errors and the automatic rate-limit retry. The flag is consumed when the call starts and adds no settings UI.
